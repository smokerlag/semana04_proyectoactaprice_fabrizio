<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:name=".ActaPriceApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Actapriceproyect">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".EstablecimientoActivity" />
        <activity android:name=".AddEstablecimientoActivity" />

        <!-- Flujo de Fiscalización -->
        <activity android:name=".FiscalizacionGeneralActivity" />
        <activity android:name=".FiscalizacionPreciosActivity" />
        <activity android:name=".FiscalizacionVerificacionActivity" />
        <activity android:name=".FiscalizacionIncumplimientosActivity" />
        <activity android:name=".FiscalizacionHechosActivity" />
        <activity android:name=".FiscalizacionFirmasActivity" />
        <activity android:name=".FiscalizacionPreviewActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>