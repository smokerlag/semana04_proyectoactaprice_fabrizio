<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvTituloIncumplimientos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Incumplimientos Detectados"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@android:color/holo_red_dark"
        android:layout_marginBottom="8dp"/>

    <TextView
        android:id="@+id/tvSubtitulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="El sistema ha detectado los siguientes hallazgos basados en la información ingresada:"
        android:textSize="14sp"
        android:layout_below="@id/tvTituloIncumplimientos"
        android:layout_marginBottom="16dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvIncumplimientosDetectados"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tvSubtitulo"
        android:layout_above="@id/btnSiguiente"
        android:layout_marginBottom="16dp" />

    <TextView
        android:id="@+id/tvEmptyIncumplimientos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="No se detectaron incumplimientos.\nPuede continuar con la fiscalización."
        android:gravity="center"
        android:textSize="16sp"
        android:visibility="gone" />

    <Button
        android:id="@+id/btnSiguiente"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:text="CONTINUAR A HECHOS VERIFICADOS" />

</RelativeLayout>
